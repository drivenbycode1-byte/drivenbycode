services:
  - type: web
    name: drivenbycode
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
    startCommand: gunicorn dbc_project.wsgi:application
    envVars:
      - fromFile: .env
